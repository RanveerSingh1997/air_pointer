name: air_pointer_example
description: Example app demonstrating air_pointer canvas input abstraction.
publish_to: none

environment:
  sdk: ^3.9.2
  flutter: '>=3.29.0'

dependencies:
  air_pointer:
    path: ..
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
